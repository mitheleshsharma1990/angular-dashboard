import { AppInfiniteScroll } from './app-infinite-scroll';

describe('AppInfiniteScroll', () => {
  it('should create an instance', () => {
    const directive = new AppInfiniteScroll();
    expect(directive).toBeTruthy();
  });
});
